{
  "name": "humancost-ai",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "HumanCost.ai — one-page dashboard: layoffs blamed on AI vs. layoffs where the company itself said AI. Plain static HTML + JSON, no build step; Node is for the browser check only.",
  "scripts": {
    "check": "node tests/site_check.mjs",
    "serve": "python3 -m http.server 8000",
    "standalone": "python3 scripts/build_standalone.py"
  },
  "devDependencies": {
    "playwright": "^1.44.0"
  }
}
